# Структура проекта PRO LAB

Детальное описание структуры проекта и назначения каждого файла.

```
prolab_ap/
│
├── README.md                    # Основная документация проекта
├── QUICKSTART.md                # Быстрый старт и справка по командам
├── DEPLOYMENT.md                # Руководство по развертыванию на сервере
├── CONFIGS_MATRIX.md            # Матрица всех конфигураций и параметров
├── EXAMPLES.md                  # Практические примеры использования
├── PROJECT_STRUCTURE.md         # Этот файл - описание структуры
├── .gitignore                   # Игнорируемые файлы Git
│
├── docs/                        # Дополнительная документация
│   └── RADIUS_SETUP.md          # Подробная настройка FreeRADIUS
│
├── hostapd/                     # Конфигурации hostapd
│   ├── common/                  # Общие файлы конфигурации
│   │   └── radius.conf          # Общие настройки RADIUS для всех конфигов
│   │
│   ├── 2g/                      # Резерв для ручных конфигов 2.4 GHz
│   ├── 5g/                      # Резерв для ручных конфигов 5 GHz
│   │
│   └── generated/               # Автоматически сгенерированные конфиги
│       ├── .gitkeep             # Placeholder для Git
│       │
│       # 2.4 GHz конфиги (7 вариантов)
│       ├── LAB-24-WPA2EAP-CCMP-PMF0.conf
│       ├── LAB-24-WPA2EAP-CCMP-PMF1.conf
│       ├── LAB-24-WPA2EAP-CCMP-PMF2.conf
│       ├── LAB-24-WPA2EAPSHA256-CCMP-PMF2.conf
│       ├── LAB-24-WPA2EAP+SHA256-CCMP-PMF1.conf
│       ├── LAB-24-WPA2EAP-GCMP-PMF1.conf
│       ├── LAB-24-WPA3EAP-SUITEB192-PMF2.conf
│       │
│       # 5 GHz конфиги (7 вариантов)
│       ├── LAB-5G-WPA2EAP-CCMP-PMF0.conf
│       ├── LAB-5G-WPA2EAP-CCMP-PMF1.conf
│       ├── LAB-5G-WPA2EAP-CCMP-PMF2.conf
│       ├── LAB-5G-WPA2EAPSHA256-CCMP-PMF2.conf
│       ├── LAB-5G-WPA2EAP+SHA256-CCMP-PMF1.conf
│       ├── LAB-5G-WPA2EAP-GCMP-PMF1.conf
│       └── LAB-5G-WPA3EAP-SUITEB192-PMF2.conf
│
├── scripts/                     # Исполняемые скрипты
│   ├── gen-enterprise-variants.sh   # Генератор всех конфигураций
│   ├── ap-run.sh                    # Запуск Access Point с конфигом
│   ├── install-to-server.sh         # Установка структуры на сервер
│   ├── test-all-configs.sh          # Автоматический прогон всех конфигов
│   ├── check-system.sh              # Проверка готовности системы
│   └── clean-generated.sh           # Очистка сгенерированных конфигов
│
└── logs/                        # Логи тестирования (создается автоматически)
    └── (автоматически создаваемые логи)
```

---

## 📋 Описание файлов

### Корневые файлы документации

#### `README.md`
- **Назначение:** Основная документация проекта
- **Содержит:**
  - Цели проекта
  - Структуру проекта
  - Быстрый старт
  - Предусловия
  - Описание вариантов конфигураций
  - Порядок работы

#### `QUICKSTART.md`
- **Назначение:** Краткая справка для быстрого старта
- **Содержит:**
  - Команды для быстрого запуска
  - Таблицу всех вариантов
  - Полезные команды
  - Решение типичных проблем

#### `DEPLOYMENT.md`
- **Назначение:** Подробное руководство по развертыванию
- **Содержит:**
  - Требования к системе
  - Установка FreeRADIUS
  - Настройка Wi-Fi интерфейса
  - Проверка установки
  - Решение проблем

#### `CONFIGS_MATRIX.md`
- **Назначение:** Детальная матрица конфигураций
- **Содержит:**
  - Таблицу всех параметров безопасности
  - Описание каждого варианта
  - Матрицу совместимости устройств
  - Тестовые сценарии

#### `EXAMPLES.md`
- **Назначение:** Практические примеры использования
- **Содержит:**
  - 13 сценариев использования
  - Примеры команд
  - Кейсы тестирования
  - Советы по отладке

#### `.gitignore`
- **Назначение:** Список игнорируемых Git файлов
- **Игнорирует:**
  - Сгенерированные `.conf` файлы
  - Логи (`.log`)
  - Временные файлы
  - OS-специфичные файлы

---

### Директория `docs/`

#### `docs/RADIUS_SETUP.md`
- **Назначение:** Подробная настройка FreeRADIUS
- **Содержит:**
  - Установка FreeRADIUS
  - Конфигурация клиентов
  - Создание пользователей
  - Настройка EAP методов
  - Генерация сертификатов
  - Отладка

---

### Директория `hostapd/`

#### `hostapd/common/radius.conf`
- **Назначение:** Общие настройки RADIUS
- **Используется:** Включается во все сгенерированные конфиги
- **Содержит:**
  - IP адрес RADIUS сервера (127.0.0.1)
  - Порты (1812 auth, 1813 acct)
  - Shared secret (testing123)
  - Базовые параметры 802.1X

#### `hostapd/2g/` и `hostapd/5g/`
- **Назначение:** Резерв для ручных конфигов
- **Использование:** Можно добавлять собственные конфиги

#### `hostapd/generated/`
- **Назначение:** Автоматически сгенерированные конфиги
- **Создается:** Скриптом `gen-enterprise-variants.sh`
- **Содержит:** 14 конфигов (7 для 2.4GHz + 7 для 5GHz)
- **Игнорируется в Git:** Да (файлы .conf)

---

### Директория `scripts/`

#### `scripts/gen-enterprise-variants.sh`
- **Назначение:** Генератор конфигураций
- **Параметры (через env):**
  - `IFACE` - Wi-Fi интерфейс (по умолчанию: wlx001f0566a9c0)
  - `OUT` - Директория вывода (по умолчанию: ../hostapd/generated)
  - `COMMON` - Путь к radius.conf
- **Создает:** 14 конфигов hostapd
- **Можно запускать:** Локально или на сервере

#### `scripts/ap-run.sh`
- **Назначение:** Запуск Access Point
- **Параметры:** Путь к конфиг файлу
- **Действия:**
  - Отключает управление NetworkManager
  - Запускает hostapd в debug режиме (-dd)
- **Использование:** `./ap-run.sh /path/to/config.conf`

#### `scripts/install-to-server.sh`
- **Назначение:** Автоматическая установка на сервер
- **Действия:**
  1. Создает `/opt/prolab/` структуру
  2. Копирует файлы из Git репозитория
  3. Устанавливает права
  4. Генерирует конфиги
- **Требует:** sudo права

#### `scripts/test-all-configs.sh`
- **Назначение:** Автоматический прогон всех конфигов
- **Параметры:**
  - `$1` - Длительность теста каждого конфига в секундах (по умолчанию: 30)
  - `CONFIGS_DIR` - Директория с конфигами
  - `LOG_DIR` - Директория для логов
- **Создает:** Логи в `logs/` директории
- **Использование:** `./test-all-configs.sh 30`

#### `scripts/check-system.sh`
- **Назначение:** Проверка готовности системы
- **Проверяет:**
  - Установленное ПО
  - Wi-Fi интерфейс и его возможности
  - FreeRADIUS
  - Структуру проекта
  - Права на файлы
- **Выход:** 0 - система готова, 1 - есть ошибки

#### `scripts/clean-generated.sh`
- **Назначение:** Очистка сгенерированных конфигов
- **Действия:** Удаляет все `.conf` из `hostapd/generated/`
- **Сохраняет:** `.gitkeep` файл

---

### Директория `logs/`

Автоматически создается скриптом `test-all-configs.sh`.

**Формат имени логов:**
```
<SSID>_<YYYYMMDD>_<HHMMSS>.log
```

**Пример:**
```
LAB-24-WPA2EAP-CCMP-PMF0_20260204_153045.log
```

---

## 🔄 Жизненный цикл файлов

### 1. Разработка (на GitHub)
```
1. Создание/изменение скриптов и документации
2. Коммит в Git
3. Push на GitHub
```

### 2. Развертывание (на сервере)
```
1. git clone с GitHub
2. Запуск install-to-server.sh
3. Создание структуры в /opt/prolab/
4. Генерация конфигов
```

### 3. Использование
```
1. Запуск FreeRADIUS
2. Запуск hostapd с выбранным конфигом
3. Тестирование
4. Логирование результатов
```

### 4. Обновление
```
1. Изменения в GitHub репозитории
2. git pull на сервере
3. Повторный запуск install-to-server.sh
```

---

## 📊 Связи между файлами

```
gen-enterprise-variants.sh
    │
    ├─→ использует: hostapd/common/radius.conf
    │
    └─→ создает: hostapd/generated/*.conf

ap-run.sh
    │
    └─→ запускает: hostapd с указанным конфигом

test-all-configs.sh
    │
    ├─→ использует: hostapd/generated/*.conf
    ├─→ вызывает: ap-run.sh для каждого конфига
    │
    └─→ создает: logs/*.log

install-to-server.sh
    │
    ├─→ копирует: hostapd/common/* → /opt/prolab/hostapd/common/
    ├─→ копирует: scripts/* → /opt/prolab/scripts/
    │
    └─→ вызывает: gen-enterprise-variants.sh

check-system.sh
    │
    ├─→ проверяет наличие: scripts/*.sh
    ├─→ проверяет наличие: hostapd/common/radius.conf
    │
    └─→ проверяет наличие: hostapd/generated/*.conf
```

---

## 🎯 Рекомендуемый workflow

### Для разработчика

1. Клонировать репозиторий
2. Внести изменения в скрипты/документацию
3. Тестировать локально с `gen-enterprise-variants.sh`
4. Коммит и push на GitHub

### Для администратора сервера

1. Клонировать репозиторий на сервер
2. Запустить `install-to-server.sh`
3. Настроить FreeRADIUS (см. `docs/RADIUS_SETUP.md`)
4. Запустить `check-system.sh` для проверки
5. Начать тестирование

### Для тестировщика

1. Запустить `check-system.sh`
2. Запустить FreeRADIUS в отдельном терминале
3. Использовать `ap-run.sh` для ручного тестирования
4. Или `test-all-configs.sh` для автоматического
5. Анализировать логи из `logs/`

---

## 🔐 Важные замечания

1. **Файлы `.conf` в `generated/` не коммитятся в Git** - они генерируются на каждой машине
2. **Скрипты должны быть исполняемыми** (`chmod +x`)
3. **RADIUS настройки** (`testing123`) - только для тестирования!
4. **Wi-Fi интерфейс** `wlx001f0566a9c0` - может отличаться на разных машинах
5. **Структура `/opt/prolab/`** создается только на сервере, не в Git

---

## 📝 Список TODO для развития проекта

- [ ] Добавить вариации по ширине канала (20/40/80 MHz)
- [ ] Создать "Ultimate WiFi Lab Map" таблицу
- [ ] Автоматизировать сканирование и сравнение
- [ ] Добавить поддержку WPA3-Personal вариантов
- [ ] Создать web-интерфейс для управления
- [ ] Интеграция с системой непрерывного тестирования
