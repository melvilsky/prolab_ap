# 🚀 PRO LAB - Enterprise WiFi Testing

Простая тестовая лаборатория для Enterprise WiFi (WPA2/WPA3-Enterprise).

---

## ⚡ Быстрый старт

```bash
./lab.sh
```

Выбрать конфиг → нажать Enter → готово!

---

## 📦 Что включено

**14 готовых конфигураций:**
- WPA2-Enterprise (базовый, SHA256, mixed)
- PMF (disabled/optional/required)
- Cipher suites (CCMP, GCMP, GCMP-256)
- WPA3-Enterprise Suite-B 192
- Для 2.4 GHz и 5 GHz

**Все конфиги готовы** - не нужно ничего генерировать!

---

## 🔧 Установка

### Локально (для тестов)

```bash
git clone <repo-url> prolab_ap
cd prolab_ap
./lab.sh
```

### На сервере

```bash
# 1. Установить зависимости
sudo apt install -y hostapd freeradius

# 2. Настроить FreeRADIUS
sudo tee -a /etc/freeradius/3.0/clients.conf <<EOF
client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    nas_type = other
}
EOF

sudo tee -a /etc/freeradius/3.0/users <<EOF
testuser    Cleartext-Password := "testpass"
EOF

# 3. Клонировать и запустить
git clone <repo-url> /opt/prolab
cd /opt/prolab
./lab.sh
```

---

## 🎯 Использование

### Терминал 1: FreeRADIUS
```bash
sudo freeradius -X
```

### Терминал 2: PRO LAB
```bash
./lab.sh
# → Выбрать: 1 (Запустить AP)
# → Выбрать конфиг по номеру
# → Нажать Enter
```

### Подключение клиента
- **SSID:** LAB-24-WPA2EAP-CCMP-PMF1 (или другой)
- **Security:** WPA2-Enterprise
- **EAP method:** PEAP
- **Phase 2:** MSCHAPv2
- **Username:** testuser
- **Password:** testpass

---

## 📊 Таблица конфигураций

### 2.4 GHz

| № | SSID | AKM | PMF | Описание |
|---|------|-----|-----|----------|
| 1 | LAB-24-WPA2EAP-CCMP-PMF0 | WPA-EAP | off | Старые устройства |
| 2 | LAB-24-WPA2EAP-CCMP-PMF1 | WPA-EAP | optional | **Рекомендуется** |
| 3 | LAB-24-WPA2EAP-CCMP-PMF2 | WPA-EAP | required | Современные |
| 4 | LAB-24-WPA2EAPSHA256-PMF2 | SHA256 | required | Усиленная безопасность |
| 5 | LAB-24-WPA2EAP+SHA256-PMF1 | Mixed | optional | Переходный |
| 6 | LAB-24-WPA2EAP-GCMP-PMF1 | WPA-EAP | optional | GCMP (если поддерживается) |
| 7 | LAB-24-WPA3EAP-SUITEB192-PMF2 | Suite-B | required | WPA3-Enterprise |

_+ аналогичные 7 для 5 GHz_

---

## 🎨 Меню lab.sh

```
1) 🚀 Запустить AP              - Выбрать и запустить конфиг
2) 📊 Показать все конфиги      - Цветная таблица
3) 🔄 Сгенерировать конфиги     - Перегенерация (если нужно)
4) ✅ Проверить систему          - Диагностика
5) 🧪 Автотест всех конфигов    - Прогон по очереди
6) ⚙️  Настройки                 - Изменить Wi-Fi интерфейс
7) 📚 Документация              - Справка
q) Выход
```

---

## 🔧 Команды напрямую

Если не хотите использовать меню:

```bash
# Запуск конкретного конфига
./scripts/ap-run.sh hostapd/generated/LAB-24-WPA2EAP-CCMP-PMF1.conf

# Автотест всех (30 сек каждый)
./scripts/test-all-configs.sh 30

# Проверка системы
./scripts/check-system.sh

# Генерация конфигов заново
./scripts/gen-enterprise-variants.sh
```

---

## 💡 Настройка

### Изменить Wi-Fi интерфейс

```bash
# Автоматически определяется, но можно указать:
export WIFI_IFACE=wlan0
./lab.sh
```

### Изменить канал

Отредактировать `scripts/gen-enterprise-variants.sh`:
```bash
CH_24="1"   # для 2.4 GHz (1, 6, 11)
CH_5="149"  # для 5 GHz (36, 40, 149, 153)
```

Перегенерировать: `./scripts/gen-enterprise-variants.sh`

---

## ⚠️ Решение проблем

### hostapd не запускается
```bash
sudo nmcli dev set wlx001f0566a9c0 managed no
sudo killall hostapd wpa_supplicant
sudo rfkill unblock wifi
```

### FreeRADIUS не отвечает
```bash
# Проверить, запущен ли
ps aux | grep freeradius

# Тест
echo "User-Name=testuser,User-Password=testpass" | \
  radclient -x 127.0.0.1:1812 auth testing123
```

### Некоторые конфиги не работают
**Нормально!** GCMP и Suite-B поддерживаются не всеми адаптерами.

Проверить поддержку:
```bash
iw list | grep -A 10 "Supported Cipher"
```

---

## 📝 Что дальше?

1. ✅ Протестировать все 14 конфигов
2. ✅ Проверить совместимость устройств
3. ✅ Изучить параметры в сканере WiFi
4. ✅ Сравнить производительность

---

## 📚 Дополнительная информация

### Детальная настройка FreeRADIUS

Файл: `/etc/freeradius/3.0/mods-available/eap`
```conf
eap {
    default_eap_type = peap
    
    peap {
        default_eap_type = mschapv2
        virtual_server = "inner-tunnel"
    }
}
```

Сертификаты:
```bash
cd /etc/freeradius/3.0/certs
sudo make
```

### VLAN Assignment

Файл: `/etc/freeradius/3.0/users`
```conf
admin    Cleartext-Password := "adminpass"
         Tunnel-Type := VLAN,
         Tunnel-Medium-Type := IEEE-802,
         Tunnel-Private-Group-Id := 100
```

### Сканирование WiFi

```bash
# Linux
sudo iw dev wlan0 scan | grep -A 20 "LAB-"
nmcli dev wifi list

# Детальная информация
sudo iw dev wlan0 scan | grep -A 30 "LAB-24-WPA2EAP-CCMP-PMF2"
```

---

## 🎓 Примеры использования

### Тест совместимости со старым устройством

```bash
# 1. PMF off (старые должны подключиться)
./lab.sh → выбрать LAB-24-WPA2EAP-CCMP-PMF0

# 2. PMF optional (все должны подключиться)
./lab.sh → выбрать LAB-24-WPA2EAP-CCMP-PMF1

# 3. PMF required (старые НЕ подключатся)
./lab.sh → выбрать LAB-24-WPA2EAP-CCMP-PMF2
```

### Тест производительности

```bash
# CCMP
./lab.sh → выбрать LAB-5G-WPA2EAP-CCMP-PMF1
# На клиенте: iperf3 -c <server> -t 60

# GCMP (если поддерживается)
./lab.sh → выбрать LAB-5G-WPA2EAP-GCMP-PMF1
# На клиенте: iperf3 -c <server> -t 60

# Сравнить результаты
```

### Автотест всех конфигов

```bash
# Запустить FreeRADIUS в терминале 1
sudo freeradius -X

# Автотест в терминале 2 (каждый по 30 сек)
./scripts/test-all-configs.sh 30

# Логи сохранятся в logs/
ls -lt logs/
```

---

## 📦 Структура проекта

```
prolab_ap/
├── lab.sh                        ← Главный скрипт (запускайте это!)
├── README.md                     ← Этот файл
│
├── hostapd/
│   ├── common/radius.conf        ← Настройки RADIUS
│   └── generated/                ← 14 готовых конфигов
│
└── scripts/
    ├── ap-run.sh                 ← Запуск AP
    ├── gen-enterprise-variants.sh ← Генератор конфигов
    ├── test-all-configs.sh       ← Автотест
    ├── check-system.sh           ← Проверка системы
    └── install-to-server.sh      ← Установка на сервер
```

---

## 📖 Параметры безопасности

### AKM (Authentication and Key Management)

- **WPA-EAP** - стандартный Enterprise
- **WPA-EAP-SHA256** - с SHA256 (усиленный)
- **Mixed** - оба одновременно
- **Suite-B-192** - WPA3-Enterprise 192-bit

### PMF (Protected Management Frames)

- **0 (disabled)** - для старых устройств
- **1 (optional)** - рекомендуется
- **2 (required)** - только современные

### Cipher Suites

- **CCMP** - стандартный AES (поддерживается везде)
- **GCMP** - быстрее на 802.11ac/ax
- **GCMP-256** - для Suite-B

---

## 🔍 Что видит сканер

При сканировании WiFi **ВИДНО:**
- ✅ SSID
- ✅ WPA2/WPA3
- ✅ Enterprise (802.1X)
- ✅ AKM (WPA-EAP vs SHA256)
- ✅ Cipher (CCMP/GCMP)
- ✅ PMF (802.11w)

**НЕ ВИДНО:**
- ❌ EAP метод (PEAP/TTLS/TLS)
- ❌ RADIUS адрес
- ❌ Сертификаты

---

## 🌐 Совместимость устройств

| Устройство | WPA2-EAP | PMF | SHA256 | GCMP | Suite-B |
|------------|----------|-----|--------|------|---------|
| Windows 10/11 | ✅ | ✅ | ✅ | ✅ | ⚠️ |
| macOS 10.13+ | ✅ | ✅ | ✅ | ✅ | ❌ |
| Linux (modern) | ✅ | ✅ | ✅ | ✅ | ⚠️ |
| iOS 8+ | ✅ | ✅ | ✅ | ⚠️ | ❌ |
| Android 6+ | ✅ | ✅ | ✅ | ⚠️ | ❌ |
| IoT устройства | ✅ | ❌ | ❌ | ❌ | ❌ |

✅ Полная | ⚠️ Частичная | ❌ Нет

---

## 📄 Лицензия

MIT License - используйте как хотите!

---

## 💬 Поддержка

Проблемы?
1. `./scripts/check-system.sh` - диагностика
2. Раздел "Решение проблем" выше
3. Проверить логи: `journalctl -u freeradius -f`

---

**Начните с:** `./lab.sh` 🚀

_Enterprise WiFi тестирование стало простым!_
