# 🚀 Установка и запуск на Linux

Пошаговая инструкция для быстрого старта.

---

## 📋 Шаг 1: Установить зависимости

### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install -y hostapd freeradius freeradius-utils git
```

### Fedora/RHEL:
```bash
sudo dnf install -y hostapd freeradius freeradius-utils git
```

### Arch Linux:
```bash
sudo pacman -S hostapd freeradius git
```

---

## 📥 Шаг 2: Скачать проект

### Вариант A: С GitHub (если уже загружен)
```bash
cd ~
git clone https://github.com/ваш-username/prolab_ap.git
cd prolab_ap
```

### Вариант B: Скопировать с флешки/другого компьютера
```bash
# С флешки
cp -r /media/usb/prolab_ap ~/
cd ~/prolab_ap

# Или через scp с другого компьютера
scp -r user@other-pc:/path/to/prolab_ap ~/
cd ~/prolab_ap
```

### Вариант C: Уже есть в /Volumes/orico/Projects/prolab_ap
```bash
# Просто скопируйте эту папку на Linux
```

---

## 🔧 Шаг 3: Настроить FreeRADIUS

### 3.1. Добавить клиента (hostapd)

```bash
sudo nano /etc/freeradius/3.0/clients.conf
```

Добавить в конец файла:
```conf
client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}
```

Сохранить: `Ctrl+O`, `Enter`, `Ctrl+X`

### 3.2. Добавить тестового пользователя

```bash
sudo nano /etc/freeradius/3.0/users
```

Добавить в начало файла:
```conf
testuser    Cleartext-Password := "testpass"
            Reply-Message := "Welcome to PRO LAB"
```

Сохранить: `Ctrl+O`, `Enter`, `Ctrl+X`

### 3.3. Проверить конфигурацию

```bash
sudo freeradius -CX
```

Должно завершиться без ошибок.

---

## 📡 Шаг 4: Узнать имя Wi-Fi интерфейса

```bash
iw dev
```

Или:
```bash
ip link show
```

Вы увидите что-то вроде:
- `wlan0`
- `wlp3s0`
- `wlx001f0566a9c0`

**Запомните это имя!**

---

## 🎯 Шаг 5: ЗАПУСК!

### Терминал 1: Запустить FreeRADIUS

```bash
# Остановить сервис (если запущен)
sudo systemctl stop freeradius

# Запустить в debug режиме
sudo freeradius -X
```

Должно появиться:
```
...
Ready to process requests
```

**Оставьте этот терминал открытым!**

---

### Терминал 2: Запустить PRO LAB

```bash
cd ~/prolab_ap

# Указать ваш Wi-Fi интерфейс (если отличается)
export WIFI_IFACE=wlan0  # замените на ваш

# Запустить меню
./lab.sh
```

**Что делать в меню:**
1. Нажать `2` — посмотреть все конфиги
2. Нажать `1` — запустить AP
3. Выбрать конфиг (рекомендую `2` — WPA2EAP-PMF1)
4. Нажать `Enter`

**Готово!** AP запущен! 🎉

---

## 📱 Шаг 6: Подключить устройство

На телефоне/ноутбуке:

1. Искать WiFi сети
2. Найти сеть: `LAB-24-WPA2EAP-CCMP-PMF1` (или другую)
3. Выбрать её

**Настройки подключения:**
- **Security:** WPA2-Enterprise (802.1X)
- **EAP method:** PEAP
- **Phase 2 authentication:** MSCHAPv2
- **CA certificate:** (не проверять / игнорировать)
- **Identity/Username:** `testuser`
- **Password:** `testpass`

4. Подключиться

**Должно подключиться!** ✅

---

## 🛑 Остановка

### В терминале с hostapd:
Нажать `Ctrl+C`

### В терминале с FreeRADIUS:
Нажать `Ctrl+C`

---

## 🔄 Запуск другой конфигурации

После остановки:

```bash
./lab.sh
# Выбрать: 1
# Выбрать другой номер конфига
# Enter
```

---

## 🧪 Автоматический тест всех конфигов

```bash
# Терминал 1: FreeRADIUS (уже запущен)
sudo freeradius -X

# Терминал 2: Автотест (каждый конфиг по 20 секунд)
cd ~/prolab_ap
./scripts/test-all-configs.sh 20
```

Все 14 конфигов запустятся по очереди!

Логи сохранятся в `logs/`

---

## ⚠️ Возможные проблемы

### Проблема 1: "Operation not permitted"

**Причина:** NetworkManager управляет интерфейсом

**Решение:**
```bash
sudo nmcli dev set wlan0 managed no
# или
sudo systemctl stop NetworkManager
```

---

### Проблема 2: "Could not configure driver mode"

**Причина:** Интерфейс занят или не поддерживает AP режим

**Решение:**
```bash
# Убить процессы
sudo killall hostapd wpa_supplicant

# Разблокировать WiFi
sudo rfkill unblock wifi

# Проверить поддержку AP режима
iw list | grep -A 10 "Supported interface modes"
# Должно быть: * AP
```

---

### Проблема 3: FreeRADIUS не отвечает

**Причина:** Неправильная настройка

**Решение:**
```bash
# Тест подключения
echo "User-Name=testuser,User-Password=testpass" | \
  radclient -x 127.0.0.1:1812 auth testing123

# Должно быть: Access-Accept
```

---

### Проблема 4: Конфиги не найдены

**Причина:** Не сгенерированы (редко, обычно уже в Git)

**Решение:**
```bash
./scripts/gen-enterprise-variants.sh
```

---

## 📊 Проверка системы

Перед запуском можно проверить всё ли готово:

```bash
./scripts/check-system.sh
```

Покажет что работает, а что нужно исправить.

---

## 🎓 Полезные команды

```bash
# Посмотреть запущенные сети
iw dev wlan0 info

# Посмотреть подключенных клиентов
sudo iw dev wlan0 station dump

# Сканировать WiFi (с другого интерфейса)
sudo iw dev wlan1 scan | grep -A 20 "LAB-"

# Логи FreeRADIUS
sudo journalctl -u freeradius -f

# Проверить порты RADIUS
sudo netstat -tulpn | grep 1812
```

---

## 📁 Структура после установки

```
~/prolab_ap/                    ← Ваш проект
├── lab.sh                      ← Запускайте это!
├── README.md                   ← Документация
├── hostapd/
│   ├── common/radius.conf
│   ├── generated/              ← 14 конфигов
│   └── custom/                 ← Ваши ручные конфиги
└── scripts/                    ← Полезные скрипты
```

---

## 🎯 Краткая шпаргалка

```bash
# 1. Установить
sudo apt install hostapd freeradius git

# 2. Настроить FreeRADIUS (один раз)
sudo nano /etc/freeradius/3.0/clients.conf  # добавить client
sudo nano /etc/freeradius/3.0/users         # добавить testuser

# 3. Узнать интерфейс
iw dev

# 4. Запуск (каждый раз)
# Терминал 1:
sudo freeradius -X

# Терминал 2:
cd ~/prolab_ap
export WIFI_IFACE=wlan0  # ваш интерфейс
./lab.sh
```

---

## ✅ Готово!

Теперь у вас есть полноценная лаборатория для тестирования Enterprise WiFi! 🚀

**Вопросы?** Смотрите `README.md` или запускайте `./scripts/check-system.sh`
